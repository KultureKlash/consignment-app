// Re-export from new location for backward compatibility
export {
  checkRateLimit,
  getClientIp,
  loginRateLimit,
  portalApiRateLimit,
  portalFormRateLimit,
} from "./system/rate-limit.server";
